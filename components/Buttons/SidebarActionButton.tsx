import { memo } from "react";

interface Props {
  handleClick: React.MouseEvent<HTMLButtonElement>;
  children: React.ReactElement;
}

export function SidebarActionButton({ handleClick, children }: Props) {
  return;
}
